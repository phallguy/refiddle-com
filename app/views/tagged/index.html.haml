%h1.page-title= title "Tagged Fiddles"

%ul.list-unstyled
  - @refiddles.each do |refiddle|
    %li= render refiddle

= paginate @refiddles

- content_for :asside do
  %h4 Tags
  %ul.tag-cloud
    - @tags.each do |tag|
      %li><= link_to tag, tagged_path(tag)